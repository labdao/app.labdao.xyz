import { Button, ButtonGroup } from '@chakra-ui/react'

const ConnectButton = () => {
    return (
        <Button colorScheme='green'>
            Connect Wallet
        </Button>
    )
}

export default ConnectButton;